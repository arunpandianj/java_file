pipeline {
    agent { label 'java_agent' }
    stages {
        stage('Build') {
            steps {
                script {
                sh 'echo "hello"
                }
            }
        }
    }
}
